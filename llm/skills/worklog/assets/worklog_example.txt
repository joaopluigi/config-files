# worklog — add a --dry-run flag to the deploy command

goal: print every side-effecting step the deploy would take, while changing
      nothing on disk or on the server

plan items
  1. understand the deploy flow and list every side-effecting step
  2. thread a --dry-run flag through the deploy and guard each step
  3. test: dry-run writes nothing; a normal run is unchanged

── log ──
00:00 #1 think two ways to reach the flag from each step: a dynamic dry-run? var, or a field on the deploy-context map already threaded through every step
00:01 #1 find deploy builds a deploy-context map and passes it to every step — src: src/deploy.clj:31-72
00:02 #1 decide put :dry-run? on deploy-context; every step already receives it, so no hidden global; rejected a dynamic var — it hides the dependency and is awkward to test — src: src/deploy.clj:31-72
00:03 #1 done three side-effecting steps found: upload!, restart!, migrate! — src: src/deploy.clj:40,55,68
00:05 #2 find the deploy runbook requires a dry run to make no calls to production — src: https://confluence.example.com/display/OPS/Deploy+Runbook
00:07 #2 think guard shape: under :dry-run?, log what the step would do and return instead of doing it — one check at the top of each step
00:09 #2 question should --dry-run contact the server read-only for an accurate plan, or stay fully offline? blocking — it changes the flag's contract
00:12 #2 answer user: stay fully offline; a dry run makes no network calls
00:14 #2 done flag parsed, put on deploy-context, all three steps guarded to log-and-return under :dry-run?
00:15 #3a plan+ also assert migrate! issues no DB call on a dry run (adds plan item 3a)
00:21 #3 done deploy_test.clj added: dry-run writes nothing and makes no network call; the normal path is unchanged; bb test green
00:22 #3a done migrate! no-DB-call assertion added and passing
00:22 #3 note verified: bb test passes and bb lint is clean
